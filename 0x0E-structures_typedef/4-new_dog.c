#include "dog.h"
#include <stdio.h>
/**
 * _strlen - a dog struct
 * @str: initial the dog
 * Return: void
*/

dog_t *new_dog(char *name, float age, char *owner)
{
	int length = 0;

	while (*str++)
		length++;
	return (length);
}
